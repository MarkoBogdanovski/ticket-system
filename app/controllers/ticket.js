const newTicket = require('./tickets/newTicket');
const assignTicket = require('./tickets/assignTicket');
const resolveTicket = require('./tickets/resolveTicket');

module.exports = {
  newTicket,
  assignTicket,
  resolveTicket
}
